{
  "name": "vira-the-clutch-connection",
  "version": "1.0.0",
  "description": "VIRA – The Clutch Connection: Smart Resource Allocation Platform",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev"
  }
}
